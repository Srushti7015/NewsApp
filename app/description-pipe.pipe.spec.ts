import { DescriptionPipe } from './description-pipe.pipe';

describe('DescriptionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
